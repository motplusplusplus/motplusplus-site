/** Single source of truth for the +1 Trash price-reveal password.
 *  Used by the /trash lightbox (TrashPageShell.tsx) and /pricelist —
 *  do not duplicate this string elsewhere. */
export const PRICE_REVEAL_PASSWORD = 'virtuesofhumantrash';
